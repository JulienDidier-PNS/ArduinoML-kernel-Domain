application redButton

# Declaring bricks
sensor button: 5
actuator led: 6
actuator buzzer: 7

# Declaring states
led {
    buzzer <= LOW
    led <= HIGH
    button is HIGH => alloff
}

-> alloff {
    led <= LOW
    button is HIGH => buzz
}

buzz {
    buzzer <= HIGH
    button is HIGH => led
}
