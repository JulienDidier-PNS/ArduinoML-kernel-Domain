application redButton

# Declaring bricks
sensor buttonA : DIGITAL
actuator led : DIGITAL

# Declaring states
on {
    led <= HIGH
    NOT (buttonA is HIGH OR buttonA is LOW)  => off
}

-> off {
    led <= LOW
    buttonA is HIGH => on
}
